## Code for calculating the binomial coefficient

def factorial(n):
    
    if n ==0 or n ==1:
        return 1
    else:
        return n * factorial(n-1)

def binom(n,k):
    if n>=k:
        return factorial(n)//(factorial(k)*factorial(n-k))
    return  'Wrong input!  𝑛  should be greater or equal to  𝑘 .'
    
    
## Test code
binom(5,3)
